let age = 20;
if ( age >= 18 ) {
    console.log( "You are eligible to vote." );
} else {
    console.log( "You are not eligible to vote." );
}

// Output: You are eligible to vote.
// Explanation: The condition age >= 18 is true, so the code inside the if block is executed, printing "You are eligible to vote."